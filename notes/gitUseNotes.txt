// 删除远程地址
git remote rm origin

// 更换远程地址
git remote add origin

// 拉取代码
git pull <remote address>  <branch>

// 生成公匙和私钥
ssh-keygen -t rsa -C

// 查看分支
git branch

// 从分支上下载
git checkout

//从远程下载
git pull = git fetch + git merge

// 从工作空间向分支提交
git commit

// 改变基线
git rebase